{
  "name": "dhanraksha",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:market": "npm run dev --prefix market-data",
    "dev": "concurrently -k -n FRONTEND,MARKET -c blue,green \"npm:dev:frontend\" \"npm:dev:market\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
